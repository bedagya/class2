var getKeys = function(obj){
    var keys = [];
    for(var key in obj){
       keys.push(key);
    }
    return keys;
 }
function isObject(obj){
    var student={
        name:"David Rayy",
        sclass:"VI",
        rollno:12 };
    }
console.log(_keys({name, sclass, rollno}));


